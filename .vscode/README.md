# VS Code Setting Files for Individuals

Team-shared VS Code settings are managed in devcontainer/devcontainer.json.
This directory is used to manage VS Code settings that are specific to each individual developer using their local machine with virtual environment.

The following files are included:

* settings.json
* extensions.json
* launch.json (if needed)